[project]
name = "etsy-bi-analyzer"
version = "0.0.1"
description = "Desktop BI tool for Etsy shop CSV exports"
requires-python = ">=3.11"
license = { file = "LICENSE" }
authors = [{ name = "Your Name" }]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
testpaths = ["tests"]